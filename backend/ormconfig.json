{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "synchronize": true,
  "username": "admin",
  "password": "admin",
  "database": "todo-list",
  "entities": ["./src/models/*.ts"],
  "migrations": ["./src/database/migrations/*.ts"],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
